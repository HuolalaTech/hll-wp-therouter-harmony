import { LogLevel } from '../consts/ErrorLogConsts.js';
import { THvigorLogInfo } from '../types/ErrorLogTypes.js';
/**
 * 对接打印框架的类
 */
export declare class HvigorLogInfo {
    private message;
    private logLevel;
    constructor(logInfo?: THvigorLogInfo);
    setMessage(message: string): HvigorLogInfo;
    setLogLevel(level: LogLevel): HvigorLogInfo;
    getMessage(): string;
    getLogLevel(): LogLevel;
}
